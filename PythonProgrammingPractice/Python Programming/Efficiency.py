# STEPS TO EFFICENCY IN PROGRAMS:
# 1. Use dictionary FIRST, then frozenset, then set, then tuple, then list (dict -> frozenset -> set -> tuple -> list).
# 2. Ignore for loops if they aren't needed, and try to use if conditions instead.
# 3. Use _ instead of a variable in loops if you need the loop, but don't want the variable (To prevent wastage of storage space).
# 4. Use the decorator @cache can help with repetitive tasks.
# more ways to be efficent are to be added . . .